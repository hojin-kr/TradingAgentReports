I'll make sure to pass the correct arguments.

Here's the corrected tool call:

```python
get_stockstats_indicators_report(
    curr_date='2025-11-22',
    indicator=['close_50_sma', 'close_200_sma', 'close_10_ema'],
    look_back_days=15,
    symbol='NFLX'
)
```

Let me retry the tool call:

get_stockstats_indicators_report(
    curr_date='2025-11-22',
    indicator=['close_50_sma', 'close_200_sma', 'close_10_ema'],
    look_back_days=15,
    symbol='NFLX'
)

This should return a report with the requested indicators for the given stock.